import styled from 'styled-components'

export const Buttons = styled.section`
  display: flex;
  place-items: center;
  place-content: center;
  gap: 24px;

  > button {
    height: 40px;
    padding: 0 16px;

    font-size: 16px;
  }
`
